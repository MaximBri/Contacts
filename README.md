# Работа по переводу `Redux Toolkit + RTK Query` на `MobX`:

Всего есть 3 хранилища: контакты, группы, любимые контакты.

Миграция была достаточно лёгкой, `MobX` не требует большого написания кода, и достаточно прост в использовании.

Данные контактов и групп размещены на `Mokky.dev`
