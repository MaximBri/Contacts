# Список замеченных проблем:

1. При нажатии на кнопки "Группы" и "Избранное" в хэдере происходит перезагрузка всего приложения. Реакт приложение не должно так реагиовать

2. В компонент каждой страницы передаются стейты, которые в будущем вызовут проблему пропс дриллинга. Кроме этого, уже на текущий момент не все страницы нуждаются во всех стейтах, которые в них передаются.

3. В компонентах были лишние импорты, которые не использовалось. Вроде мелочь, но немного очищает компонент

---

Прочее:

- Подключён `Redux`, убрана вся лишняя логика внутри страниц, код в них стал чище.

- Сделано автоматическое добавление данных в хранилище после загрузки спустя секунду (имитируем запрос)

- Добавлена ленивая подгрузка страниц
